    # entry.asm — PROVIDED. Kernel entry point. Sets up sp to boot_stack_top
    # and jumps into Rust's `rust_main`.
    .section .text.entry
    .globl _start
_start:
    la sp, boot_stack_top
    call rust_main

    .section .bss.stack
    .globl boot_stack_lower_bound
boot_stack_lower_bound:
    .space 4096 * 16
    .globl boot_stack_top
boot_stack_top:
